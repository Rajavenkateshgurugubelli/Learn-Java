package oop.abstraction;

/**
 * Module 02: Abstraction
 * Topic: Interface
 * 
 * Objectives:
 * 1. Define an interface (contract).
 * 2. All methods are implicitly public and abstract (before Java 8).
 */
public interface Drawable {
    void draw();
}
