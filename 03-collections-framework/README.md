# Module 03: Collections Framework

The Java Collections Framework provides a set of classes and interfaces to store and manipulate groups of data.

## 🎯 Objectives
- Understand the Hierarchy (`Collection` interface).
- Master `List` for ordered collections.
- Use `Set` for unique elements.
- Store key-value pairs with `Map`.
- Handle queues with `Queue` and `Deque`.
- Sort collections using `Comparable` and `Comparator`.

## 📂 Content Overview

| File | Description |
|---|---|
| `ListInterface.java` | `ArrayList` (fast access) vs `LinkedList` (fast manipulation). |
| `SetInterface.java` | `HashSet` (unique, unordered), `TreeSet` (sorted), `LinkedHashSet` (ordered). |
| `MapInterface.java` | `HashMap`, `TreeMap`, `LinkedHashMap`. |
| `QueueAndDeque.java` | `PriorityQueue` (heap) and `ArrayDeque` (stack/queue). |
| `SortingAndComparable.java` | Custom sorting logic. |

## 🚀 Key Interfaces
1.  **List**: Ordered, allows duplicates.
2.  **Set**: Unique, no duplicates.
3.  **Map**: Key-Value pairs (not a subtype of Collection).
4.  **Queue**: FIFO (First-In-First-Out) operations.
