# Module 05: Modern Java Features

Start writing cleaner, more functional code with features introduced in Java 8 and beyond.

## 🎯 Objectives
- Use **Lambda Expressions** for concise code.
- Understand standard **Functional Interfaces** (`Predicate`, `Consumer`, `Function`, `Supplier`).
- Process collections declaratively with the **Stream API**.
- Avoid `NullPointerException` with **Optional**.
- Handle dates and times correctly with the **Time API** (Java 8+).

## 📂 Content Overview

| File | Description |
|---|---|
| `Lambdas.java` | Syntax of lambda expressions (`->`). |
| `FunctionalInterfaces.java` | Using built-in functional interfaces. |
| `StreamsAPI.java` | Filtering, mapping, and reducing data. |
| `OptionalClass.java` | Handling nulls gracefully. |
| `DateTimeAPI.java` | `LocalDate`, `LocalTime`, `LocalDateTime`, `ZonedDateTime`. |

## 🚀 Key Concepts
- **Functional Programming**: Treating computation as the evaluation of mathematical functions.
- **Lazy Evaluation**: Streams are lazy; they don't process data until a terminal operation is called.
- **Immutability**: The new Date/Time classes are immutable and thread-safe.
