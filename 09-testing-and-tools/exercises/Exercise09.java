package testing.exercises;

/**
 * Module 09: Exercise
 * 
 * Challenge: String Utilities Test
 * 
 * 1. Create a class `StringUtils` with a static method `isPalindrome(String
 * str)`.
 * - Should return true for "madam", "Racecar" (ignore case).
 * - Should return false for "hello".
 * - Should throw IllegalArgumentException if null.
 * 
 * 2. Create a test class `StringUtilsTest` (in the same file or separate).
 * - Test `isPalindrome` with valid palindromes.
 * - Test with non-palindromes.
 * - Test with null input (assertThrows).
 */
public class Exercise09 {
    // TODO: Implement StringUtils and StringUtilsTest
}
