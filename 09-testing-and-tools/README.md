# Module 09: Testing and Build Tools

Professional development requires robust testing and efficient build management.

## 🎯 Objectives
- Understand the structure of a **Maven** project.
- Write unit tests using **JUnit 5**.
- Use **Assertions** to verify code behavior.
- Use **Mockito** to isolate components during testing.

## 📂 Content Overview

| File | Description |
|---|---|
| `MavenProjectStructure/` | Example `pom.xml`. |
| `JUnit5Basics.java` | Annotations: `@Test`, `@BeforeEach`, `@AfterEach`. |
| `AssertionsExample.java` | `assertEquals`, `assertTrue`, `assertThrows`. |
| `Mocking.java` | Using Mockito to mock dependencies. |

## 🚀 Key Concepts
- **Unit Testing**: Testing individual components in isolation.
- **TDD (Test Driven Development)**: Writing tests *before* writing the code.
- **Mocking**: Simulating complex dependencies (like a Database) to test logic quickly.
